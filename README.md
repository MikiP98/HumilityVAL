# HumilityVAL

### Humility Version Agnostic Library

## Roadmap

- Consider bringing back `Settings Duplication` for `BlockRegistrar`, 
  but using Manifold's `@JailBreak` and vanilla MC settings copy.